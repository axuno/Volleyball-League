REM If Visual Studio shows "not installed" under the "npm" tree although modules ARE installed,
REM just call this (nodejs.exe assumed to be in PATH), 
REM and after a restart of Visual Studio, everything shows fine:
npm install
