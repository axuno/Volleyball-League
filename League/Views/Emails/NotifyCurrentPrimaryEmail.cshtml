@inject IViewLocalizer Localizer
@{
    Layout = "_LayoutEmail";
}
@using Microsoft.AspNetCore.Mvc.Localization
@*ValueTuple*@
@model (string Email, string CallbackUrl, TournamentManager.MultiTenancy.ITenantContext TenantContext)
<div>
    <h3>@Localizer["Your primary email"]</h3>
    <p>
        @Localizer["Your primary email is about to be changed to"]<br />
        <strong>@Model.Email</strong>
    </p>
    <p>
        @Localizer["We have sent a confirmation code to the new email address"].
    </p>
    <p>
        @Model.TenantContext.OrganizationContext.Name
    </p>
</div>
